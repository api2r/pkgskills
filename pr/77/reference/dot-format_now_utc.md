# Format the current time as a UTC timestamp string

Format the current time as a UTC timestamp string

## Usage

``` r
.format_now_utc()
```

## Value

(`character(1)`) Current time formatted as `"YYYY-MM-DD HH:MM:SS UTC"`.
