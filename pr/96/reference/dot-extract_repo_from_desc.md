# Extract owner and repo from DESCRIPTION BugReports field

Extract owner and repo from DESCRIPTION BugReports field

## Usage

``` r
.extract_repo_from_desc(call = caller_env())
```

## Arguments

- call:

  (`environment`) The caller environment for error messages.

## Value

(`list`) Named list with `owner` and `repo` elements.
