# use_skill_document() emits message when aaa-shared_params.R is created

    Code
      use_skill_document(open = FALSE)
    Message
      Skill '.github/skills/document/SKILL.md' installed.
      'R/aaa-shared_params.R' created.
      i Add shared parameters to it as your package grows. Functions can inherit them with `@inheritParams .shared-params`.

